apiVersion: apps/v1
kind: Deployment
metadata:
  name: http-nats-proxy
spec:
  replicas: 3
  selector:
    matchLabels: {app: http-nats-proxy}
  template:
    metadata:
      labels: {app: http-nats-proxy}
    spec:
      terminationGracePeriodSeconds: 45
      securityContext:
        runAsNonRoot: true
        seccompProfile: {type: RuntimeDefault}
      containers:
        - name: proxy
          image: registry.example.com/http-nats-proxy@sha256:REPLACE_ME
          ports:
            - {name: http, containerPort: 8080}
          envFrom:
            - secretRef: {name: integration-gateway-secrets}
            - configMapRef: {name: integration-gateway-config}
          readinessProbe:
            httpGet: {path: /health/ready, port: http}
            periodSeconds: 5
          livenessProbe:
            httpGet: {path: /health/live, port: http}
            periodSeconds: 10
          resources:
            requests: {cpu: 250m, memory: 256Mi}
            limits: {cpu: "2", memory: 1Gi}
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities: {drop: ["ALL"]}
---
apiVersion: policy/v1
kind: PodDisruptionBudget
metadata:
  name: http-nats-proxy
spec:
  minAvailable: 2
  selector:
    matchLabels: {app: http-nats-proxy}
