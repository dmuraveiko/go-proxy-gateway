# Security

Report vulnerabilities privately to the owning security team; do not open public issues containing credentials or payloads. Secrets, wallet keys and NATS seeds must never enter this repository. Production requires authenticated NATS, TLS, key-scoped command authorization, provider-specific webhook verification and restricted egress.
